sudo: required
language: generic
services: 
  - docker

before_install:
  - docker build -t yadjanor/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true yadjanor/docker-react npm run test